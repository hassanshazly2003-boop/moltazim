name: moltazim
description: تطبيق ملتزم - يستخدم للورد والصلاة والاذكار
publish_to: 'none'
version: 1.0.0+3

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - quran.json
    - qurantext-uthmani.source.def
    - assets/logo.jpg
